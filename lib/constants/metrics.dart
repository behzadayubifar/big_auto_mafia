import 'package:flutter/material.dart';
import 'package:flutter_hooks/flutter_hooks.dart';

class Metrics {
  Metrics._();

  static const titleTextHeight = 100.0;
  static const titleTopPadding = 56.0;
  static const titleBottomPadding = 64.0;
  // static final heightSize = MediaQuery.of(_context).size.height;
  // static final widthSize = MediaQuery.of(_context).size.width;

  // static BuildContext get _context => useContext();
}
