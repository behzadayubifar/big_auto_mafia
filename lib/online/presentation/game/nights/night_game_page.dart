import 'package:flutter/widgets.dart';

class NightGamePage extends StatelessWidget {
  const NightGamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
