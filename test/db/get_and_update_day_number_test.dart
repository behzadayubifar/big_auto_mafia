// import 'package:test/test.dart';

// void main() {
//   group('GameStatusCollection', () {
//     test('Update and Get Day Number', () async {
//       // Create a new instance of the game status collection
//       final gameStatusCollection = GameStatusCollection();

//       // Set the initial day number
//       final initialDayNumber = 1;
//       await gameStatusCollection.updateDayNumber(initialDayNumber);

//       // Get the day number from the game status collection
//       final dayNumber = await gameStatusCollection.getDayNumber();

//       // Verify that the day number is equal to the initial day number
//       expect(dayNumber, equals(initialDayNumber));

//       // Update the day number
//       final updatedDayNumber = 2;
//       await gameStatusCollection.updateDayNumber(updatedDayNumber);

//       // Get the updated day number from the game status collection
//       final updatedDayNumberResult = await gameStatusCollection.getDayNumber();

//       // Verify that the updated day number is equal to the new day number
//       expect(updatedDayNumberResult, equals(updatedDayNumber));
//     });
//   });
// }
